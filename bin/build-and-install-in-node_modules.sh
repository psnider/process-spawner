SOURCES='package.json process-spawner.d.ts'
TARGET=node_modules/@sabbatical/process-spawner
rm -fr ${TARGET}
mkdir -p ${TARGET}
cp ${SOURCES} ${TARGET}
tsc -p .
cp ./process-spawner.js ${TARGET} 
tsc -p tsconfig.tests.json
